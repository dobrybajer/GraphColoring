<controls:MetroWindow  x:Class="GraphColoring.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
                       MinHeight="600" MinWidth="800" ResizeMode="CanResizeWithGrip" Background="#FF979797" ShowIconOnTitleBar="False" ShowMaxRestoreButton="False" ShowMinButton="False" ShowTitleBar="False"
         >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Name="ContentPanel" Margin="0" Background="WhiteSmoke">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding ConsoleOutput, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path=.}" Style="{StaticResource ConsoleTextBlock}" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBox Text="{Binding ConsoleInput, Mode=TwoWay}" Background="GhostWhite" Name="InputBlock" BorderBrush="{x:Null}" SelectionBrush="{x:Null}" Style="{StaticResource ConsoleTextBox}" TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Column="1">
            <TextBlock Style="{StaticResource Settings}" Text="Ustawienia algorytmu" MinHeight="20" TextWrapping="WrapWithOverflow" ></TextBlock>
            <TextBlock Style="{StaticResource Settings}" Text="Wersja algorytmu:" MinHeight="20" TextWrapping="WrapWithOverflow" ></TextBlock>
            <StackPanel HorizontalAlignment="Left" Width="Auto" VerticalAlignment="Top" Orientation="Horizontal">
                <Label Content="GPU"/>
                <CheckBox Name="Gpu" VerticalContentAlignment="Center" />
                <Label Content="CPU (Table)"/>
                <CheckBox Name="CpuT" VerticalContentAlignment="Center" />
                <Label Content="CPU (Bit)"/>
                <CheckBox Name="CpuB" VerticalContentAlignment="Center" />
             </StackPanel>
            <TextBlock Style="{StaticResource Settings}" Text="Wybrana ścieżka (plik lub folder):" MinHeight="20" TextWrapping="WrapWithOverflow" ></TextBlock>
            <TextBlock Style="{StaticResource Settings}" Text="{Binding Path}" MinHeight="20" TextWrapping="WrapWithOverflow" ></TextBlock>

         
   
            <StackPanel Height="20"  HorizontalAlignment="Center" Width="Auto" Orientation="Horizontal">
                <Button Style="{StaticResource SettingsButton}" Width="90" Margin="0,0,10,0" Content="Wybierz folder" Click="ChooseFolder_Click"/>
                <Button Style="{StaticResource SettingsButton}" Width="90" Content="Wybierz plik" Click="ChooseFile_Click"/>
            </StackPanel>
            <TextBlock Style="{StaticResource Settings}" Text="Ustawienia konsoli" MinHeight="20" TextWrapping="WrapWithOverflow" ></TextBlock>
            <StackPanel Height="20"  HorizontalAlignment="Center" Width="Auto" Orientation="Horizontal">
                <Button Style="{StaticResource SettingsButton}" Width="90" Margin="0,0,10,0" Content="Czyść konsolę" Click="ClearLog_Click"/>
                <Button Style="{StaticResource SettingsButton}" Width="90" Content="Utwórz plik log" Click="MakeLog_Click"/>
            </StackPanel>
            <Button Style="{StaticResource SettingsButton}" Width="90" Content="Uruchom" Click="Run_Click"/>
        </StackPanel>
        
        <!--<graphColoring:CommandWindow Grid.Row="0" x:Name="ContentPanel" Style="{StaticResource ContentPanel}" Height="Auto" UseLayoutRounding="True"/>-->
        <!--<Grid Grid.Row="1" Name="ContentPanel" Style="{StaticResource ContentPanel}" Height="Auto"/>-->
  
    </Grid>
</controls:MetroWindow>
